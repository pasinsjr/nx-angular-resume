import { AuthLoaded } from './auth.actions';
import { AuthState, initialState, authReducer, IUser } from './auth.reducer';

describe('Auth Reducer', () => {
  const getAuthId = it => it['id'];
  let createAuth;
});
